#include <stdio.h>

// 1. Program to accept two numbers and compute their addition.
int main() {
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    printf("Sum: %d\n", a + b);
    return 0;
}
